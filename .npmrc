# The overrides in package.json force better-auth to the security-patched
# line (>=1.6.23, see GHSA advisories against <=1.6.12), which conflicts with
# @neondatabase/auth's exact internal peer pin of better-auth@1.4.18.
# Remove once the Neon SDK ships with a patched better-auth.
legacy-peer-deps=true
