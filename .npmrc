# @neondatabase/auth (beta) pins peerOptional next>=16; the SDK works on
# Next 14 (see docs middleware note for "earlier versions"). Revisit when
# upgrading Next or when the SDK leaves beta.
legacy-peer-deps=true
